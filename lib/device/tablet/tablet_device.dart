import 'package:DevFolio/device/tablet/tablet_portrair.dart';
import 'package:flutter/material.dart';

class TabletDevice extends StatelessWidget {
  const TabletDevice({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return TabletPortrait();
  }
}
